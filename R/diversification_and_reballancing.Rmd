---
title: An attempt at optimal allocation
author: Daniel R. Williams ^[Director of Quality Control, Greenstreet Growers, Lothian,
  MD] ^[PhD Candidate, Dept. of Ag. & Crop Science, Ohio State University, Columbus,  Ohio]
date: 2 August 2024
output:
  html_notebook: default
---


```{r setup}
# this block loads required packages and some custom functions. 

cran_pacs <- c()
dev_pacs <- c() 
bioc_pacs <- c()
py_pacs <- c()

# install pak
if(!nzchar(find.package("pak", quiet = TRUE))) install.packages("pak")

## function to quietly find list of packages
# find_packages_quiet <- function(pkgs, ...) unlist(find.package(pkgs, quiet = TRUE, ...))
# install_as_needed <- function(pacs, inst_fun = pak::pak, check_fun = find_packages_quiet, ...) {
#   # inst_fun(setdiff(pacs,check_fun(pacs)),...)
#   inst_fun(setdiff(pacs,names(unlist(sapply(pacs,\(x) check_fun(x))))),...)
# }

# install R pacs
r_pacs <- c(cran_pacs,dev_pacs,bioc_pacs) |> unique()
if (length(r_pacs) > 0) pak::pak(r_pacs)

```

